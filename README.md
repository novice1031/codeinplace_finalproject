# codeinplace_finalproject
A text-based search engine
The searchengine.py is the main python file
"bbcnews" and "small" are the files which are to be indexed and the queries input by the user are looked for in these files
If the queries are present within these files, you'll get the info
If not, you'll get query doesn't match any result
